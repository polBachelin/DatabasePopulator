package schema

// func BuildDocument(block *BlockData) (bson.M, error) {
// 	document := make(map[string]interface{})

// 	for _, field := range block.Fields {
// 		document[field.Name] =
// 	}

// }

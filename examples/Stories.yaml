blocks:
  - name: Stories
    fields:
      - name: category
        type: string
      - name: isDraft
        type: boolean
      - name: movie
        type: object
        fields:
          - name: release_date
            type: date
